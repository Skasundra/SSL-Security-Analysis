{
  "name": "ssl-security-analysis",
  "version": "1.0.0",
  "main": "Backend/app.js",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "npm install --prefix Backend && npm install --prefix Frontend && npm run build --prefix Frontend && node verify-build.js",
    "start": "NODE_ENV=production node Backend/app.js",
    "dev": "cd Backend && npm run dev",
    "verify": "node verify-build.js",
    "heroku-postbuild": "npm install --prefix Backend && npm install --prefix Frontend && npm run build --prefix Frontend"
  },
  "keywords": ["ssl", "security", "analysis", "certificate"],
  "author": "",
  "license": "ISC",
  "description": "SSL Security Analysis Tool with React Frontend and Express Backend"
}
